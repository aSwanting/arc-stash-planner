services:
  diff-explorer:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8787:8787"
    environment:
      NODE_ENV: production
      PORT: 8787
    env_file:
      - .env
